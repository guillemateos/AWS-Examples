tasks:
  - init: |
      # Install AWS CLI and tree
      sudo apt-get update
      sudo apt-get install -y awscli
      sudo apt-get install -y tree

      # Set AWS credentials directly in AWS CLI if available as environment variables
      if [[ -n "$AWS_ACCESS_KEY_ID" ]] && [[ -n "$AWS_SECRET_ACCESS_KEY" ]]; then
        aws configure set aws_access_key_id "$AWS_ACCESS_KEY_ID"
        aws configure set aws_secret_access_key "$AWS_SECRET_ACCESS_KEY"
        aws configure set region "us-east-1"  # Or use your preferred AWS region
      fi
  - command: |
      echo "AWS CLI and tree installed."
      tree --version

# Setting environment variables (ensure these are available in your Gitpod workspace)
env:
  AWS_ACCESS_KEY_ID: "AKIATWBJ2PT3V4XTMXGY"
  AWS_SECRET_ACCESS_KEY: "048ZD1/GEK8bowZ7FYbMs/PaPgCD9B1BqNl+rpaK"